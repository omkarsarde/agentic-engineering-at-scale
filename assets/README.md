# Visual assets

- `diagrams/source/` stores editable diagram source when it is not embedded Mermaid or DOT.
- `diagrams/rendered/` stores exported SVG fallbacks.
- `figures/` stores plots generated by chapter artifacts.

Every rendered asset must retain its source and generation command. Do not commit a screenshot as the only representation of a diagram.

